export function SummonerDTO(data: any) {
  this.accountId = data.accountId;
  this.profileIconId = data.profileIconId;
  this.revisionDate = data.revisionDate;
  this.name = data.name;
  this.id = data.id;
  this.puuid = data.puuid;
  this.summonerLevel = data.summonerLevel;
}
